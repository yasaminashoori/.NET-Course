using System;

namespace Kamel_boodan
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("ENter a number:");
            int input1 = convert.TOint32(console.ReadLine());

            // var result = input1 / 2 + input1 / 3 + input1
            for (int i = 1; i < input1 ; i++)
            {
                result = input1 / i;
                result = 
            }

        }
    }
}
